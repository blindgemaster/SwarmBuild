# Swarmbuild — Web Frontend

Next.js frontend for Swarmbuild. See the [root README](../../README.md) for full setup instructions.

## Quick Start

```bash
# From the repo root
npm install
npm run dev:web
```

Runs on [http://localhost:3000](http://localhost:3000). Requires environment variables — see `.env.example`.
